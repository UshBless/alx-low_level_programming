#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strcpy - copy the character of an alphabet
 * @dest: character to copy
 * @src: character to copy
 * Return: zero
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
